{ pkgs, attrs, config, ... }:
{
  fileSystems = {
    # server drive
    # "/storage" = {
    #   device = "/dev/disk/by-label/WEBSTORAGE";
    #   fsType = "ext4";
    #   neededForBoot = false;
    # };
    # "/lgass" = {
    #   device = "/dev/disk/by-label/LGASS";
    #   fsType = "ext4";
    #   neededForBoot = false;
    # };
  };
}